Go back to documentation `Homepage <index.html>`_

This is the list of fallspeed models used by `snowProperties <snowProperties.html>`_ to compute the terminal fallspeed

By default the Boehm1992 model is used

The keyword arguments of the models such as the thermodynamic properties of the air are set by default, but can be overloaded by passing a dictionary of arguments

Fallspeed models
================

.. automodule:: snowScatt.fallSpeed._fallSpeedModels
   :members: